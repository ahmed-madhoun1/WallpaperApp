<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.WallpaperApp" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/customColor2</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/customColor2</item>
        <!-- window Background -->
        <item name="android:windowBackground">@color/customColor</item>
        <!-- text Color Primary -->
        <item name="android:textColorPrimary">@color/white</item>
        <!-- text Color Secondary -->
        <item name="android:textColorSecondary">@color/white</item>
    </style>

</resources>